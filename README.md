How to use ? 
1. Clone this repo 
2. Install Postgresql and open pgadmin.
    1. Change the username and password in the application.properties as per local DB creds
    2. Create a database with the name feature_flag
3. Do mvn clean install (Make sure the build passed)
4. Go to FeatureFlowApplication and click run.

